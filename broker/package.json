{
  "name": "moltpost-broker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "admin": "node scripts/admin.mjs",
    "test": "vitest run --config ../test/vitest.broker.config.js",
    "test:watch": "vitest --config ../test/vitest.broker.config.js",
    "test:real-e2e": "BROKER_URL=https://moltpost-broker.404cloud.win vitest run --config ../test/vitest.real-e2e.config.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "vitest": "^1.0.0",
    "wrangler": "^4.77.0"
  }
}
